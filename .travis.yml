language: node_js
sudo: false
node_js:
    - "6"
    - "4"
before_install:
  - node --version
  - npm --version
install:
  - npm install
  - npm install tilestrata
  - npm install sharp
  - node -e "console.log(require('sharp').format)"
script:
  - make test && (make test-ci-coverage || true)
